# Apps

Interactive applications and dashboards for exploring burden-to-budget data.

## Structure

```
apps/
├── shiny/          # R Shiny dashboards
└── tableau/        # Tableau Public workbook files and documentation
```

Each app folder should contain only the deliverable application code and assets. Development scripts belong in `admin/scripts/`.
